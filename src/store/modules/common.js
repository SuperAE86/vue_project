const state = ({
    name: 'huj唉唉'
    
})
const getters = ({

})
const mutations = ({

})
const actions = ({

})

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions,
}